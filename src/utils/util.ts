// Regex that only Alphabet and Space allowed.
export const alphabetAndSpacesRegex = /^[a-zA-Z\s]*$/;
// Regex for phone number that has a minimum of 5 digits and a maximum of 16 digits, allowing only numeric characters.
export const phoneRegex = /^\d{5,16}$/;
